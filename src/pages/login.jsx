import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../context/AuthContext';export default function Login(){const{login}=useAuth(),nav=useNavigate(),[e,setE]=useState(''),[p,setP]=useState(''),[m,setM]=useState('');return <section className="auth-section"><form className="auth-card" onSubmit={x=>{x.preventDefault();login(e,p)?(setM('✅ Login successful!'),setTimeout(()=>nav('/'),500)):setM('❌ Invalid email or password.')}}><div className="auth-icon">🌱</div><h1>Welcome Back!</h1><label>Email<input type="email" value={e} onChange={x=>setE(x.target.value)} required/></label><label>Password<input type="password" value={p} onChange={x=>setP(x.target.value)} required/></label>{m&&<p className="success">{m}</p>}<button className="btn primary full">Login</button><div className="divider">OR</div><Link className="center-link" to="/register">Create New Account</Link></form></section>}